SELECT * FROM student
WHERE age BETWEEN 15 AND 25;

SELECT student.name FROM student;

SELECT * FROM student
WHERE name LIKE '%i%';

SELECT * FROM student
WHERE age < id;

SELECT * FROM student
ORDER BY age;
